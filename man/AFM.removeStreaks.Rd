% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AFM.removeStreaks.R
\name{AFM.removeStreaks}
\alias{AFM.removeStreaks}
\title{Remove horizontal streak artifacts from an AFM image}
\usage{
AFM.removeStreaks(obj, no = 1, threshold = 0.4, verbose = FALSE)
}
\arguments{
\item{obj}{An AFM object containing raster data in \code{obj@data$z}.}

\item{no}{Integer index of the image/channel in \code{obj@data$z} to process.
Defaults to \code{1}.}

\item{threshold}{Numeric in \eqn{[0,1]} giving the fraction of the maximum
second-difference used as a detection cutoff. Larger values are more
conservative. Default is \code{0.4}.}

\item{verbose}{Logical; currently unused. Default \code{FALSE}.}
}
\value{
A modified copy of \code{obj} with \code{obj@data$z[[no]]} updated and
  \code{obj@history} appended.
}
\description{
Detects stripe/streak artifacts by thresholding a discrete second-difference
along the \emph{y} direction (row-wise) and replaces flagged pixels with the
value from the next row. The operation is recorded in the object's history.
}
\details{
The discrete second-difference is computed for interior rows as
\eqn{2 m[i,] - m[i-1,] - m[i+1,]}. Values above \code{threshold * max(.)} are
flagged as streak pixels. Flagged pixels are replaced with the pixel directly
below (row \code{i+1}) at the same column.
}
\examples{
f <- AFM.getSampleImages()[2]
a <- AFM.import(f)
a2 <- AFM.removeStreaks(a)
plot(a2)

}
\author{
Thomas Gredig
}
